server:
  port: 0

spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

eureka:
  client:
    enabled: false

logging:
  level:
    root: WARN
    com.shopscale: INFO